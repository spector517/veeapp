#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
    remove|upgrade|deconfigure)
        systemctl stop veeapp-backend.service || true
        systemctl disable veeapp-backend.service || true
        ;;
esac

exit 0
